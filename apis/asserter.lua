local asserter = {}

function assert_form(val, exp)
  local result = true;
  if type(val) == type(exp) then
    if type(val) == "table" then

    else

    end
  end
  return result;
end

return asserter;
