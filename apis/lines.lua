local lines = {}
lines.line = {
}

function lines:new_line(...)
  local m = {...}
  if type(m[1])=="table" and type(m[2])=="table" and type(m[1][1])=="number" then

end
function lines.get_intersection()

return lines;
